Config = {}

-- Framework options: "auto", "qbcore", "qbox", "esx", "standalone"
--   "auto"      = auto-detect (recommended for most setups)
--   "qbcore"    = force QBCore
--   "qbox"      = force Qbox (qbx-core)
--   "esx"       = force ESX
--   "standalone"= no framework
Config.Framework = "auto"

-- Auto-detect on startup (set to false to use only the value above)
Config.AutoDetect = true
